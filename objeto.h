#ifndef OBJETO_H
#define OBJETO_H

#include <stdio.h>
//Preprocessamento-------------------------------------------------------------------

void analisarotulo(Tokens *ponteiro);
int trataconst(Token *ponteiro);
void ChecaTokenSintatico(Tokens *ponteiro);

#endif
